'''
  * @author   remonl
  * @coding    utf-8
'''

一.集合
1. 特性：无序且不重复的数据组合
2. 优点：去重（把列表变成集合，就自动去重了）；关系测试（测试两组数据之前的交集、差集、并集等关系）
二.装饰器
定义:本质是函数，目的是为其它函数添加功能
原则：1. 不能修改被修饰的函数的源代码；2. 不能修改被装饰器的函数的调用方式
实现装饰器的知识储备
1. 函数即“变量”
附加知识：形参与实参
形参：函数名后边括号里的变量即为形参
实参：调用函数时传递的值或者变量即为实参
举例：
def test(a,b): -----> 这里的 a 和 b 就是形参
    return a+b
test(1,2)---> 这里的 1,2 就是实参

x=1,y=2
test(x,y)---> 这里的 x,y 就是实参
2.高阶函数（满足下列之一即为高阶函数）
    a. 把一个函数名当做实参传给另一个函数(在不修改被修饰函数的情况下为其添加附加功能)；
    b. 返回值中包含函数名
3.嵌套函数
高阶函数 + 嵌套函数 ---> 装饰器

第四周---第09章节开始
